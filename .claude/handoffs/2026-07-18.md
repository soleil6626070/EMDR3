# EMDR3 Handoff — 2026-07-18

For a fresh agent continuing work on `/home/aidan/Projects/EMDR3` (Lua / LÖVE2D EMDR therapy app).

## State: clean and pushed

- Branch: `main`, up to date with `origin/main` at `7ad7b8d`. Working tree clean. No other work in flight.
- Stale local branches `processing_loop` and `transcription` exist (plus their remotes and `origin/elevenlabs`); user was offered cleanup but hasn't confirmed — check merge status before deleting.

## What this session did (read the commits for detail, they're thorough)

1. `0ebf7ad` — Pre/post SUD (0–10) rating screens + per-session JSON records under `output_data/targets/<slug>/sessions/`. Replaced the old flat `session_*.txt` output and the never-implemented "linked list" plan.
2. `f14f070` — Rewrote `.claude/CLAUDE.md` and README to match the actual code (they were badly stale).
3. `9b2a036` — Implemented session resume after crash **or** Escape (Escape = pause, by user decision). Old "Resume previous session?" prompt had been deleted in `dcbc35e`; this rebuilds it properly (richer marker, off-by-one fixed, replays cue-in on resume).
4. `7ad7b8d` — Flipped the session-record single writer from the whisper worker to the **main thread**. Worker is now a pure transcriber (WAV in → text out via status channel); main thread saves results + ratings and deletes WAVs. Net −19 lines. Do **not** flip this back — rationale in CLAUDE.md "Key Data Structures" and CONSIDERATIONS #6.

All live-tested by the user: full 6-cycle session (record verified at `output_data/targets/awkward_puddle_moment/sessions/session_20260718_181433.json`) and the resume flow.

## Authoritative docs (all current as of `7ad7b8d`)

- `.claude/CLAUDE.md` — architecture, flow mermaid, single-writer rule, Session Resume section, Known Loose Ends. Trust this.
- `README.md` — UX + technical flowcharts, per-session dev log (convention: add an entry per work session).
- `CONSIDERATIONS.md` + `TODO.md` — **gitignored, local-only**. Open items: #2 (mic failure still silent — user *believes* it's fixed; it isn't, only root cause #3 was) and #4 (missing `ELEVENLABS_AGENT_ID_EMDR` setup cliff). #1/#3/#5/#6 resolved.

## Likely next work (user's stated intentions, none started)

- **UX flowchart redesign** — the user's original goal this session, deferred. CLAUDE.md flow is now accurate as a baseline to redesign from.
- **TII agent behavior fixes** (TODO.md): conversation ends after first body sensation; sections need intros; direct target-image questioning never happens; possible stage reorder.
- Whisper hallucinates on silent recordings (e.g. `"(upbeat music)"`) — options discussed: bigger model (`ggml-medium.en`) or filter bracketed non-speech annotations.
- Old flat `session_*.txt` / `target_image_*.txt` files in `output_data/` predate the JSON format; left as-is deliberately.

## Working notes for the next agent

- User is not a programmer — explain technical choices clearly, in plain prose; they engage well with AskUserQuestion for genuine design decisions and will push back usefully (their "is that overengineered?" led to the `7ad7b8d` simplification).
- User cares that work is *pushed*, not just committed (got burned by unpushed `elevenlabs` branch).
- No lua/luajit on PATH. Verify pattern used twice this session: write a headless LÖVE test app (conf.lua with `t.window = false`, modules disabled) in the scratchpad, `loadfile()` for syntax checks + behavior assertions, then `timeout 6 love .` from the project root for a boot check (no output = clean; LÖVE prints errors to stdout). Mic/audio flows need the user to live-test.
- Secrets live in `.env` (gitignored): ElevenLabs/OpenAI/Anthropic keys, agent id. Never print or commit.
- Commit convention: `Co-Authored-By: Claude <model> <noreply@anthropic.com>` trailer; README session-log entry per work session.

## Suggested skills

- `verify` — before committing nontrivial changes; drive the affected flow (headless LÖVE pattern above).
- `run` — to launch the app (`love .` from project root).
- `code-review` — after any substantial feature, before push.
- `context7-mcp` — **only if the user explicitly asks**; library IDs are pinned in CLAUDE.md (`/websites/love2d_wiki`, `/websites/elevenlabs_io`, etc.).
- `codebase-design` — useful for the upcoming UX-flow redesign discussion.
